name: example
description: An example of how to use the dart_mp_analytics package.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_mp_analytics:
    path: ../
